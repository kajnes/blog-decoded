<% include ./partials/header %>

<div class="ui main text container">
    <div class="ui huge header">decoded</div>
    <div class="ui top attached segment">
        <div class="ui divided items">
            <% posts.forEach(function(post){   %>

<div class="item">
    
      <a class="ui tiny image" href="<%= post.slug %>"> <img src="<%= post.image %>"></a>
   
   <div class="content">
       <a class="header" href="<%= post.slug %>"><%= post.title %>  </a>
       <div class="meta"><span><%= post.created.toDateString() %></span></div>
              <div class="description"><p><%- post.lead %></p></div>
              
    </div>
    
    
</div>

<% }) %>
        </div>
    </div>
</div>







<% include ./partials/footer %>